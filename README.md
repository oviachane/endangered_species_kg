# Endangered Species Knowledge Graph

A Semantic Web project intended to crawl web pages, extract information about endangered species, and build a foundational Knowledge Graph.

## Environment Setup
```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```
